Step 0: created repo on github which provided initial commit.

Step 1: git add README.md
		git add STEPS.md
		git commit -m "Step 1."
		git push -u origin main

Step 2: git add STEPS.md
		git commit -m "Step 2."
		git push -u origin main

Step 10:<br>
git add STEPS.md<br>
git commit -m "Step 10."<br>
git push -u origin main<br>